[project]
name = "doorboard-event-client"
version = "0.0.0"
requires-python = ">=3.12"
dependencies = ["doorboard-contracts"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/doorboard_event_client"]
