# scripts

Repo-level developer scripts (created from T-000 onward): `dev-up` (simulated stack), `lint`, `typecheck`, `test`, `check-structure` (validates task briefs and doc links — used by CI). Scripts are thin wrappers; logic belongs in the tools/services themselves.
