#!/usr/bin/env bash
set -euo pipefail

source "$(dirname "$0")/_uv-env"

pnpm_cmd=(pnpm)
if ! command -v pnpm >/dev/null 2>&1; then
  pnpm_cmd=(corepack pnpm)
fi

"$UV_PROJECT_ENVIRONMENT/bin/ruff" format --check .
"$UV_PROJECT_ENVIRONMENT/bin/ruff" check .
"${pnpm_cmd[@]}" exec eslint .
